<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h2 class="mb-4">Kavyata Blog</h2>
        <a href="/articals/new" class="btn btn-success">Create Artical</a>

        <% artical.forEach(artical=>{%>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%=artical.title%></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%=artical.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2"><%=artical.dec%></div>
                    <a href="articals/<%=artical.slug%>"  class="btn btn-primary">Read</a>
                    <a href="/articals/edit/<%=artical.id%>" class="btn btn-info">Edit</a>

                    <form action="/articals/<%=artical.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>

            </div>
        <%})%>
    </div>

</body>
</html>